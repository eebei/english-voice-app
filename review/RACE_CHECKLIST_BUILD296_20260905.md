# 本日午前のテストレース — Build 296

公開中: **Build 296**（自動更新で入る。タイトルバーで確認）
前回走行: Build 295（2026-09-04 夕・Le Mans/IMSA）で退行3件

---

## 何が変わったか（前回の走行との差）

| # | 前回の症状 | 直した内容 |
|---|---|---|
| 1 | **GAPが出ない**「後ろのGAPはまだ取れていない」 | クラスを跨いで順位を比較していた。GTP P3 と 自車GT3 P10 を `3<10` で「前方」と誤判定し、物理位置の「後方」と食い違って**2,796回黙っていた**。物理位置だけで方向を決めるようにした |
| 2 | **記憶が出ない**「前回のこのコース・このクルマはデータがない」 | タイム制レースは総周回を出すのに**平均ラップ**が要るのに、燃費しか記憶から通していなかった。平均ラップを独立経路にした |
| 3 | **「前方に停止車両。null秒。」** | 距離が無い時は距離を言わない |
| 4 | （新規） | ドライバーの発話ごとに**記憶を1回検索して回答に混ぜる**（Memory Brain 第1スライス） |

---

## 走行中に確認すること（4つだけ・数字は覚えなくていい）

| # | いつ | 言うこと／見ること | 期待 |
|---|---|---|---|
| **1** | 接近戦の最中 | **「後ろとのギャップは？」** | **黙らない。**「後ろのGT3 1.9秒」のようにクラス名つきで返る |
| **2** | いつでも（最重要） | **「プランA！何周目？」** | **周回数を答える。**「6周を走り終えてピットイン、作業は7周目。**前回の同条件燃費からの推定だよ**」 |
| **3** | グリッドで黙って聞く | — | 燃費とピット周回を**自分から**言うか |
| **4** | Luna が数字を言った直後 | **「後ろ2秒だね、ギャップ」**（訂正） | 訂正を**声で**受領するか |

### 条件が1つだけあります

**記憶は「前に走ったコース × 同じ車」でしか働きません。**

- **Le Mans × Mercedes-AMG GT3 2020** → 9/4のデータがあるので**記憶が出るはず**
- **別のコース** → 「初めてだから3周走らせて」が**正解**（数字を作ったら捏造で失敗）

---

## 本日いちばん価値がある1件

**記憶が前回なぜ落ちたのか、まだ誰も知りません。**

診断 `memory_rejection_reason` を今回追加しました。本日のログを見れば**理由が確定します**。

- **記憶が出た** → 直った
- **記憶が出ない** → **理由がログに出る**（`no_matching_record` / `fuel_burn_unavailable` / 各 mismatch）

**どちらでも収穫です。** 出ない方が、むしろ原因が分かって前に進みます。

---

## ログから当方が確認すること（Yuji は覚えなくてよい）

| 診断 | 見るもの |
|---|---|
| `GAP AUTHORITY` | `direction_conflict` が **2,796回 → 0回** になっているか／`source=physical_traffic_gap` |
| `MEMORY_ACTION` | `memory_basis` が `memory_previous` か／`avg_lap_s` に値があるか |
| `memory_rejection_reason` | **記憶が落ちた場合の理由** |
| `STRATEGY_PLAYBOOK` | `pit_entry_after_lap` / `pit_service_lap` が実際のピットと合うか |
| `INTENT_ROUTE` | 「プランA！何周目？」で**2つの intent** が返っているか |
| `DISPUTE_DETECTED` / `DISPUTE_ACK` | 訂正の軸・撤回対象・**声で受領できたか** |
| 停止車両コール | `null` の語が混ざっていないか |
| 自発GAPの表示・音声 | Overlayに出た全文と、実際に耳で聞いた全文が同じか。特に現在GAPと変化量 |
| `GAP_FRESHNESS` | `fate=rebuild was=... now=...` が出た時、Overlayも最終音声へ更新されたか |

---

## 合否は甘くしません

**内部で検出できていても、耳で聞こえなければ不成立**とします。
「Lunaが黙ってた」「変なことを言った」も立派な結果なので、そのまま伝えてください。

**Gate 6（Windows実機）/ Gate 8（実走）は、今日までの全工程で唯一まだ埋まっていない部分です。**
これまでの合格はすべて内部計算であり、`EXTERNAL_USER_DISCOVERY_SAHIDE_20260902.md` の受入条件——

> A test passes only when the **audible** driver-facing result is verified.

——にはまだ届いていません。

**走り終わったらログを渡してください。** 全部突き合わせます。

---

## 走行中の追加観察（試験項目として暗記しなくてよい）

自然な日本語で普段どおり話してください。特に次が起きた箇所は、Build 297の会話設計に使います。

- Lunaが理解できず「言い直して／もう一度説明して」と聞いた
- 一部は理解できており、曖昧な箇所だけ聞き返せた／全部を言い直させた
- Lunaが言った数字や状況をYujiが訂正した
- Lunaが自分の古い情報を明確に撤回し、新しい情報へ置き換えた／古い情報を再利用した
- 同じ作業を別の日本語表現で頼んだ時、認識結果が変わった
- 「後ろ3.4秒、1.2秒縮んだ」のような自発GAPで、Overlayと音声の数字が違った
- GAPの現在値や変化量が何度も同じ数字に聞こえた

可能なら発話の言い回しはそのまま残します。成功だけでなく、沈黙・聞き返し・誤認識が重要な教材です。

---

## Build 296実走後 → Build 297

Build 296の午前走行は**現場証拠を取る工程**です。ログを解析してからBuild 297へ反映します。

Build 297の中心は引き続き**レース会話成立**。その縦切り機能として、iRacingのピット作業を音声で扱います。

1. 自然な日本語を受ける
2. Lunaが「エンジニア用の閉じた定型命令」へ変換する
3. 曖昧な箇所だけ確認し、理解済み部分は保持する
4. 燃料などの数値はプログラムで計算・検証する
5. iRacingのピット命令へ変換する
6. 実際の予約状態を確認してから、Lunaが設定内容を復唱する
7. 訂正時は古い命令を撤回・上書きし、その事実を声で知らせる

初期対象は「タイヤなし／4輪交換／給油Xリットル／満タン／ゴール時3リットル残し／現在設定の確認／直前指示の変更・取消」。

**「ゴール時3リットル残し」は重要な製品仕様。** `#autofuel` の周回マージンへ丸投げせず、残り距離・実測燃費・ピット進入時予測残量・容量から必要給油量を決定論的に計算します。

Build 297の実装・公開内容は、本日のBuild 296ログ解析で確定します。Build／公開は従来どおり別GOです。
