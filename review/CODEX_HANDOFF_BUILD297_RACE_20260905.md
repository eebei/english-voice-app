# Codex へ：Build 297 実走の改善事項（2026-09-05）

宛先: Codex / 報告: Claude Code / 決裁: Yuji
ログ: `OMORAY-bridge-debug-20260905-0941.log`（**3.12MB / 22,737行**・**公開中の Build 297**）
詳細解析: `review/BUILD297_RACE_ANALYSIS_20260905.md`

Yuji:

> ちょっとアプデの完成度が低いぞ！**これ最大改善テーマだ！**
> **ログも見れるようにしろ。**

---

## 0. 先に当方の失敗を3件記録する

Yuji から直接指摘を受けた。**process の問題であり、次に同じことをしないために残す。**

### 失敗1 コードを読む前に「他人のデータ漏洩の可能性」と報告した

「八木さん」を見て**先に情報漏洩を疑い、そう報告した**。実際はソースの定数だった。
Yuji「これ最悪だろ。利用者が多かったら取り返しつかんぞ！**これ君の作業効率＆正確性に問題あり**」。

**1分でコードを grep すれば分かることを、先に警報として出した。** 順序が逆である。

### 失敗2 ベタ書きの定数が公開に乗ったのを、公開前に見つけられなかった

`renderer.html:4991` に `'八木さん'` がリテラルで入ったまま Build 296・297 が公開された。
**Gate 5 は同梱を確認するが、中身が正しいかは見ない。** 当方はテスト値の残存を検査していない。

### 失敗3 昨日の GAP 修正を「合格」と検証したが、実走で5倍悪化した

`direction_conflict` は **2,796回 → 14,190回**。
当方は `build_record()` に反例を投げて「speakable になった」ことだけ確認し、
**実ログを再生していない。** 2日連続で同じ失敗をしている。

---

## 1. 「八木さん」— ソースの定数 ★1行・最優先

`desktop/renderer.html:4991`:

```js
? window.PitwallPddp.briefingLine(_pddp, _decLang==='ja' ? '八木さん' : 'Driver') : ''
```

**誰が走っても「八木さん」と呼ぶ。** 記憶の混線ではない。

同じ瞬間の診断は正しい:

```
CONVO [CONVO_BOX] {"session_key":"Yuji|1|Lone Qualify|..."}
CONVO [LunaJP] 八木さん、直近10レース、平均Incidents 0.4、最新iRating 2027。
```

`userName` は同ファイルの `mirrorToOverlay()` で既に使われている。

**あわせて要求する検査：** ソース全体を走査し、**実在のテスター名・個人名がリテラルで
残っていないこと**を preflight で落とす。今回は1件だが、**同種が他に無い保証が無い。**

### 付随

ドライバーが自分の名前を2回聞いている（「ルナ、俺の名前は何？」「知ってる？」）。
**別人の名前で呼ばれたので確認せざるを得なかった**と読める。

---

## 2. ★ログを「見れるようにする」— これが2番目（直す前の段階）

Yuji の要求そのものである。**今のログは量だけ多く、肝心のことが分からない。**

| 行の種類 | 件数 | 比率 |
|---|---|---|
| **`GAP AUTHORITY:`** | **18,409** | **81%** |
| その他すべて | 4,328 | 19% |

内訳は**理由1種類のみ**（`direction_conflict_rank_vs_physical`）。

### 2-1 `GAP AUTHORITY:` に `source_kind` が無い

```
GAP AUTHORITY: behind not speakable reason=direction_conflict_rank_vs_physical idx=27
```

**18,409行あって、どの source_kind で落ちたかが1行も無い。**

そちらの修正は `source_kind == physical_traffic_gap` の時だけ順位比較を外す。
conflict が出続けるということは `same_class_battle_gap`（F2Time）に落ちているはずだが、
**それを確認する手段がログに無い。**

`_physical_signed` が None になる条件は `player_last_lap <= 20` か
**どちらかの `LapDistPct` が負**（コース外・ピット・切断）。
今回はインシデント14・3台停止の荒れたレースなので**隣接車がコース外だった可能性が高いが、断定できない。**

**要求：** `source_kind` / `target_class` / `player_class` / 両車の `LapDistPct` の有無を同じ行に出す。

### 2-2 同じ行を18,409回出す設計そのもの

`source_kind` を足しても、**毎poll全件を出せばログはさらに膨らむ。**
同一 `(direction, reason, idx, source_kind)` は**集約して回数と時間範囲で1行**にすべきである。

**「見れるようにする」＝行を増やすことではない。** 今のログは量が多くて読めない。

### 2-3 停止車両が発火しない理由の診断が無い（次項）

---

## 3. 停止車両コールが 0回 ★危険・退行

```
9/4 朝（Build 294）: 0回
9/4 夕（Build 295）: 4回      ← 出ていた
9/5   （Build 297）: 0回      ← 消えた
```

**`bridge.py` の停止車両まわりは 295→297 で1行も変わっていない**（`git diff` で確認）。
コード退行ではなく**状況依存**だが、**理由が記録されていない。**

### 実際に起きたこと

3台の事故現場で、停止車両を**GAPとして**報告している。

```
10:09:25  Luna「前10.6秒。4秒縮んだ。」→ TTS「前11.6秒。」
10:10:00  Driver「さっきの前 11.6秒 っていうのは あの 止まっている車のこと言ったんだね」
```

### Yuji の指示（文言について）

> **GAP「前11.6秒のGAP」というならOK!** これ改善だね。
> 特にこちらから尋ねた場合は簡素化でもいいけど、**Luna自身からのコールでしょ？**
> **停止車両の認識度アップは必要**、これは**過去にできていたので今回から消えた**！

つまり:

- **「前11.6秒のGAP」という言い方自体は正しい。** 何のGAPか分かる形にする＝改善項目
- **ドライバーが尋ねた時は簡素化してよい**
- **Luna 自発のコールは別。停止車両は「停止車両」として認識・通知すること**

### 要求

1. `bridge.py:5399-5460` の停止車両判定に診断を入れる
   （`car_stopped_since` の登録数、`_surf`、`_sdist`、armed 状態、20秒クールダウン）
2. **なぜ発火しなかったかがログ1行で分かる**こと
3. 直すのはその後。**推測で条件を緩めない**（誤検知は安全コールで最も危ない）

---

## 4. GAP の値そのものが候補と発話で最大2.0秒違う

今朝入れた `GAP_DISPLAY_SYNC` が記録した5件すべて:

| 候補生成時 | 実際に発話 | 差 |
|---|---|---|
| 前5.5秒。1.9秒縮んだ。 | **前7.3秒。** | **+1.8** |
| 前5.1秒。2.2秒縮んだ。 | **前7.1秒。** | **+2.0** |
| 前10.6秒。4秒縮んだ。 | 前11.6秒。 | +1.0 |

ドライバーの訂正:

```
前は 5.3秒だね。それ。
1秒ずれてるよ。5.5秒 4秒 とか。
```

Le Mans で2.0秒＝**約115m**。キューの数百msで車が115m動くことはない。
**候補側（`gap_trend`）と出口側（`GapFreshness.evaluate()` が `lastTelemetry` から読み直す値）が
別の出所である可能性が高い。** 同一発話IDで両方の出所を記録すれば確定する。

**なお表示と音声のズレ自体は今朝の修正で解消している**（5件すべて `amended_turn=true`、
ドライバーも発話された値で聞き返している）。**残っているのは値の精度。**

---

## 5. Luna が自分の質問を「記録」として撤回している

```
Luna「今回はIncidents 14。一番危なかった接触は、こちらから行った側？受けた側？」
Luna「わかった、私の"今回はIncidents 14。一番危なかった接触は…受けた側？"という記録は撤回する。」
```

**質問文全体を1つの主張として撤回対象にしている。**
9/3 の軸一致16/16は**ドライバー発話の分類**であり、**Luna 側の発話が質問か主張かは見ていない。**

---

## 6. 提案する順序

| # | 対象 | 段階 |
|---|---|---|
| **1** | 「八木さん」＋実名リテラルの preflight 検査 | **直す** |
| **2** | `GAP AUTHORITY` に `source_kind` 等を出す＋**同一理由の集約** | **見えるようにする** |
| **3** | 停止車両が発火しない理由の診断 | **見えるようにする** |
| 4 | ②③の結果を見てから GAP 沈黙と停止車両を直す | 直す |
| 5 | 候補と出口の値の出所統一 | 直す |
| 6 | 「前◯秒のGAP」の言い方／自発と応答の書き分け | 直す |
| 7 | Luna の質問を撤回対象にしない | 直す |

**②③を先にやる理由：** 昨日 GAP を推測で直して**5倍悪化**させた。同じことを繰り返さない。
**当方の失敗3がその実例である。**

---

## 7. 未確認・注意

- ④の「別の出所」は**推測**。同一発話IDで両方を記録するまで断定しない。
- ③の発火しない理由は**ログから判定不能**。
- **本レポートは実装を一切変更していない。commit・Build・公開も未実施。**
